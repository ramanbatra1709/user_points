{#
/**
 * @file user_points.html.twig
 * Default theme implementation to present User points data.
 *
 * This template is used when viewing User points pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_user_points()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('user_points') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
